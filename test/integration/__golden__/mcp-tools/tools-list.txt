{
  "result": {
    "tools": [
      {
        "name": "search_docs",
        "title": "Search documentation",
        "description": "Hybrid search over the \"handbook-project\" documentation: meaning and exact wording at once, so an identifier — an environment variable, a header, an error code — finds its page as readily as a question does. Returns the most relevant excerpts with their file path, heading breadcrumb and similarity score, each shown with the passage before and after it for context. Narrow it with source or path_prefix when you already know where the answer lives, and with version when this project holds more than one release of the same documentation. When nothing is a good match it says so rather than returning the least bad thing it found. Use read_document with a returned file path to read the whole file, or its heading breadcrumb to read just that section.",
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "minLength": 1,
              "maxLength": 2000,
              "description": "Natural-language question or keywords"
            },
            "limit": {
              "default": 5,
              "description": "Maximum number of excerpts to return (1-20, default 5)",
              "type": "integer",
              "minimum": 1,
              "maximum": 20
            },
            "source": {
              "description": "Search only this source — the first segment of the paths list_topics shows. Omit to search every source.",
              "type": "string",
              "maxLength": 64
            },
            "path_prefix": {
              "description": "Search only documents whose path starts with this, e.g. \"handbook/operations\". Omit to search the whole project.",
              "type": "string",
              "maxLength": 512
            },
            "version": {
              "description": "Search only documents of this release, e.g. \"v3\". An exact label as the project set it — there is no ordering and no \"latest\"; an unknown value is answered with the versions this project does have. Omit to search every version, which is what most projects have exactly one of.",
              "type": "string",
              "minLength": 1,
              "maxLength": 64
            }
          },
          "required": [
            "query"
          ]
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "execution": {
          "taskSupport": "forbidden"
        }
      },
      {
        "name": "list_topics",
        "title": "List documentation topics",
        "description": "Lists the indexed documents of the \"handbook-project\" project grouped by source and directory (paths are \"<source>/<path>\"), with titles and chunk counts. Use it to discover what documentation exists before searching or reading. One call returns 200 documents unless limit says otherwise (at most 1000); when more remain the answer ends with a next_cursor value to pass back as cursor for the following page.",
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "cursor": {
              "description": "Continue a previous listing: pass the next_cursor value it ended with. Omit to start at the beginning.",
              "type": "string",
              "maxLength": 2048
            },
            "limit": {
              "default": 200,
              "description": "Documents per page (1-1000, default 200)",
              "type": "integer",
              "minimum": 1,
              "maximum": 1000
            }
          }
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "execution": {
          "taskSupport": "forbidden"
        }
      },
      {
        "name": "read_document",
        "title": "Read a documentation file",
        "description": "Returns the Markdown of one indexed file of the \"handbook-project\" project. Pass the path exactly as shown by search_docs or list_topics (e.g. \"docs/guides/install.md\"; the first segment is the source). Pass heading with a breadcrumb from a search result to read only that section and the subsections under it, or from/to to read a range of chunks — either is far cheaper than a whole page. Output is capped at max_tokens (default 4000) and says where it cut and how to ask for the rest.",
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "minLength": 1,
              "maxLength": 1024,
              "description": "Relative file path as listed by search_docs / list_topics"
            },
            "heading": {
              "description": "Read only this section: a heading breadcrumb as search_docs shows it, e.g. \"Guide > Install > Docker\". Subsections included.",
              "type": "string",
              "maxLength": 512
            },
            "from": {
              "description": "First chunk index to read (0-based, as the chunk counts in list_topics are numbered)",
              "type": "integer",
              "minimum": 0,
              "maximum": 9007199254740991
            },
            "to": {
              "description": "Last chunk index to read, inclusive",
              "type": "integer",
              "minimum": 0,
              "maximum": 9007199254740991
            },
            "max_tokens": {
              "default": 4000,
              "description": "Token budget for the text returned (200-20000, default 4000)",
              "type": "integer",
              "minimum": 200,
              "maximum": 20000
            }
          },
          "required": [
            "path"
          ]
        },
        "annotations": {
          "readOnlyHint": true,
          "destructiveHint": false,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "execution": {
          "taskSupport": "forbidden"
        }
      }
    ]
  },
  "jsonrpc": "2.0",
  "id": 1
}
