# Specification sources

The `openapi` flavor expands `.yaml`, `.yml` and `.json` into one document per operation. This file is
ordinary Markdown, so it stays one document — which is the behaviour this fixture exists to pin.
